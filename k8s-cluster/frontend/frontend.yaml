apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: app-desafio
spec:
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
      - name: frontend
        image: brunosantos88/otel-app-frontend:v2